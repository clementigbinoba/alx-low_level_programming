#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
*main - Entry point
*
*Return: Always 0 (success)
*
*/
int main(void)
{
	char c;
	int d;

	c = 'a';
	while
	(d < 10)
		{
			putchar(d + '0');
			d++;
		}
	while
		(c <= 'a')
		{
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
