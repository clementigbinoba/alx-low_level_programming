#include "main.h"

/**
 * _putchar.c - writes the character to c stdout
 * print_alphabet - print all alphabet in lowercase
 */
void print_alphabet(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letters++)
		_putchar('letter');

	print_alphabet('\n');
}
