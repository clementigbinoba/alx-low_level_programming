#include "main.h"

/**
 * _islower - short discription, sinhle line
 * @c: contain value to be compared
 * Return: Always 0.
 */
int _islower(int c)
{
	if (c > 'a' && c < 'z')
	{
		return (i);
	}
	else
	{
		return (0);
	}
}
